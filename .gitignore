# Source PDFs are copyrighted course material — never commit them.
pdfs/*
!pdfs/.gitkeep

# Text and page images extracted from those PDFs are the same content in
# another format, so they are just as copyrighted. Regenerate with
# tools/extract.py instead of committing.
text/

# Local scratch
tmp/
*.local.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node (only used by tools/validate.mjs, which has no dependencies)
node_modules/
